# LEARNER_STATE

Version: 1.1
Last Updated: 2026-08-23
State Owner: 02_학습_아카이브_매니저
Learning Source: 01_AI_학습_코치
Canonical Storage: GitHub / Blast-Forge-Lab

---

## 1. 현재 학습 트랙

```text
Python
↓
AI Literacy  ← 현재 단계
↓
SQL
↓
FastAPI
↓
LLM
↓
RAG
↓
AI Agent Development
```

현재 단계: **AI Literacy 전환 시작**

Python 기초 학습은 이번 주를 기준으로 1차 학습 단계를 마치고, 다음 큰 학습 주제를 AI Literacy로 전환합니다.

Python에서 학습한 문제 구조화, 실행 흐름 추적, 오류 분석, 함수·객체·비동기 등은 종료된 지식이 아니라 이후 AI Literacy → LLM → RAG → Agent 학습에서 계속 재사용할 기반 지식으로 유지합니다.

---

## 2. 현재 학습 목표

현재 목표는 Python 문법 자체를 더 많이 암기하는 것이 아니라, **Python에서 만든 기술적 기초를 AI 개념과 실제 응용 문제에 연결하는 것**입니다.

핵심 목표:

1. AI Literacy 교안 전체 구조를 강의 전에 파악한다.
2. 새로운 AI 개념이 전체 AI 시스템에서 어떤 역할을 하는지 설명할 수 있다.
3. 비슷한 AI 용어와 개념의 차이를 구분할 수 있다.
4. 강의 중 이해되지 않는 개념을 그대로 넘기지 않고 구조적으로 질문한다.
5. 배운 개념을 실제 서비스·비즈니스 문제와 연결한다.
6. Python에서 배운 데이터·함수·객체·API·비동기 개념을 AI 학습과 연결한다.
7. AI가 생성한 설명과 코드를 그대로 받아들이지 않고 구조와 논리를 검토한다.
8. 최종적으로 AI Agent 서비스를 기획하고 구현하는 데 필요한 판단 능력을 만든다.

---

## 3. Python 학습 단계 요약

상태: `1차 학습 완료 / 반복 복습 필요`

Python 과정에서 다음 영역을 학습했습니다.

- 변수 / 자료형 / 연산자
- 문자열
- 리스트 / 튜플 / 딕셔너리 / 집합
- 조건문
- 반복문
- 함수 / 매개변수 / return
- 스코프 / 람다
- 클로저 / 데코레이터
- 이터러블 / 이터레이터 / 제너레이터
- 클래스 / 객체 / 인스턴스
- 상속 / 캡슐화
- 예외 처리 / 디버깅
- 내장 함수 / 표준 라이브러리
- 파일 입출력 / CSV / JSON
- Pandas / Matplotlib 기초
- 동기 / 비동기 / Coroutine / async / await

Python 학습의 목적은 문법 완전 암기가 아니라 이후 기술 학습에서 코드를 읽고 구조를 이해하기 위한 기초 형성이었습니다.

---

## 4. Python에서 확인된 비교적 안정적인 영역

### 기본 자료구조와 문법

상태: `안정 ~ 부분 이해`

- 변수 기본 개념
- 기본 자료형
- 기본 연산자
- 문자열
- 리스트
- 튜플
- 딕셔너리
- 집합
- 기본 인덱싱과 슬라이싱

### 조건문

상태: `부분 이해`

- 단순한 `if` 문은 이전보다 읽는 것이 익숙해짐
- 비교 연산과 기본 조건식 이해
- 반복문·함수와 결합된 조건문은 계속 복습 필요

### 반복문

상태: `부분 이해`

- `for`
- `while`
- `range`
- `break`
- `continue`
- 반복 변수
- 누적 변수의 기본 개념

추가 복습 필요:

- 문제를 보고 반복이 필요한지 판단하기
- 중첩 반복
- `continue` 실행 흐름
- 필요한 변수 개수 설계

---

## 5. 함수

상태: `부분 이해`

현재 이해한 내용:

- 함수는 반복해서 사용할 기능을 묶은 구조
- 매개변수
- 인수
- 지역변수
- `return`
- 함수 호출 후 반환값이 다른 변수에 저장될 수 있음

추가 복습 필요:

- 문제에서 함수가 필요한지 스스로 판단
- 매개변수와 실제 값의 흐름
- 여러 함수가 연결될 때 실행 순서
- 함수와 메서드의 관계

---

## 6. 클래스 / 객체지향

상태: `부분 이해 / 반복 복습 필요`

### 현재 이해

- `class`는 객체를 만들기 위한 설계도라는 개념
- 클래스 안에는 데이터와 기능을 정의할 수 있음
- 클래스 안에 정의된 함수는 메서드가 될 수 있음
- `.`은 왼쪽 대상이 가진 속성이나 기능에 접근하는 문법
- 클래스 변수는 클래스가 가지는 공용 데이터
- 인스턴스 변수는 각각의 인스턴스가 따로 가지는 데이터

### 계속 복습할 부분

- 객체 / 인스턴스 구분
- 변수와 객체의 관계
- `self`
- 인스턴스 메서드
- 클래스 메서드
- `cls`
- `@classmethod`
- `@staticmethod`
- 데코레이터
- 클래스 변수와 인스턴스 변수 탐색 방식

### 캡슐화 관련

```text
캡슐화
→ 객체 내부 데이터 접근을 관리하는 객체지향 개념

Name Mangling
→ __변수명에 대해 Python이 내부 이름을 변경하는 규칙
```

---

## 7. 예외 처리 / 디버깅

상태: `부분 이해`

### 예외 처리

- `try` / `except` 실행 흐름
- `ValueError`
- `raise`
- 예외 발생 후 흐름 이동

핵심 구분:

```text
raise
→ 예외를 발생시킴

try
→ 예외가 발생할 수 있는 코드를 실행

except
→ 발생한 예외를 처리
```

### 디버깅

- 브레이크포인트
- Variables 패널
- Step Into
- Step Out
- 실행 흐름 단계 추적

핵심 구분:

```text
예외 처리
→ 문제가 생겼을 때 프로그램의 대응 방법

디버깅
→ 문제가 생긴 이유를 찾아 수정
```

---

## 8. 최근 Python 후반 학습 영역

상태: `개념 노출 및 기초 이해`

- `all()` / `any()`
- `len()` / `sum()` / `round()`
- `mean()` / `median()`
- iterable
- 표준 라이브러리
- 파일 입출력
- CSV / JSON
- 직렬화 / 역직렬화
- Pandas / DataFrame
- Matplotlib
- CPU Bound / I/O Bound
- Blocking / Non-blocking
- Event Loop
- Coroutine
- `async` / `await`
- `asyncio.gather()`

이 영역은 이후 API, FastAPI, AI Agent 개발에서 다시 등장할 가능성이 높으므로 필요 시 맥락 안에서 재복습합니다.

---

## 9. 현재 문제 풀이 능력 관찰

### 확인된 강점

- 완성된 간단한 코드를 보면 실행 흐름을 읽을 수 있는 경우가 증가함
- 오류 메시지의 위치를 함께 보면 원인을 이해하는 능력이 향상됨
- 자신의 말로 개념을 설명하려고 시도함
- 모르는 부분을 넘어가지 않고 질문함
- 비슷한 개념의 차이를 구분하려는 질문을 자주 함

### 계속 훈련할 부분

문제를 바로 코드로 옮기기보다 먼저 구조화하는 과정.

```text
문제
↓
입력
↓
출력
↓
변수
↓
연산
↓
조건
↓
반복
↓
함수
↓
예외
↓
코드
```

이 구조화 방식은 Python이 끝나도 유지하며, AI Literacy에서는 다음 형태로 확장합니다.

```text
문제 / 현상
↓
핵심 개념
↓
입력 데이터
↓
AI가 수행할 역할
↓
출력 / 기대 결과
↓
제약 / 위험
↓
실제 서비스 적용
```

---

## 10. 현재 관찰 중인 취약 단계

### 우선 추적

`문제에서 필요한 관계를 먼저 구조화하는 단계`

Python 학습에서 완성된 코드를 보면 이해할 수 있지만, 문제 문장에서 먼저 변수·연산 관계를 뽑아내는 과정에서 어려움이 관찰되었습니다.

이를 단일 취약점으로 확정하지 않고 계속 추적합니다.

AI Literacy에서는 동일한 패턴이 다음과 같이 나타나는지 확인합니다.

- 개별 용어는 이해하지만 전체 시스템 관계를 연결하기 어려운가
- 예시는 이해하지만 새로운 사례에 적용하기 어려운가
- 설명을 들으면 이해하지만 자기 말로 다시 구조화하기 어려운가

---

## 11. 2026-08-19 Python 학습 로직 변경 기록

Python 후반부부터 다음 문제 풀이 순서를 적용했습니다.

```text
문제 확인
↓
입력값 찾기
↓
출력값 찾기
↓
필요한 변수 찾기
↓
연산 관계 만들기
↓
조건 / 반복 / 함수 / 예외 필요 여부 판단
↓
실행 순서 설계
↓
직접 코드 작성
↓
코드 검토
```

변경 이유:

정답 코드를 읽는 능력과 새로운 문제를 코드로 구조화하는 능력은 서로 다른 능력이기 때문입니다.

이 원칙은 AI Literacy에서도 **정답 설명 암기보다 개념 구조화와 응용**을 우선하는 방식으로 이어갑니다.

---

## 12. 누적 학습 확장 전략

새로운 개념을 학습할 때 이전에 안정적으로 배운 개념을 1~2개 연결합니다.

Python에서는:

```text
변수
↓
조건문
↓
반복문
↓
함수
↓
예외처리
↓
클래스
```

AI Literacy 이후에는:

```text
Python 기초
↓
데이터 / API / 비동기 기본
↓
AI Literacy 개념
↓
LLM
↓
RAG
↓
AI Agent
```

모든 개념을 한꺼번에 연결하지 않고, 현재 학습 주제에 필요한 이전 개념만 선택적으로 복습합니다.

---

## 13. AI Literacy 전환 시 연결할 Python 기반

AI Literacy 학습에서 필요할 때 다음 Python 개념을 다시 연결합니다.

- 데이터 타입 / 자료구조
- 함수와 입력·출력
- JSON
- 파일 입출력
- API와 연결될 수 있는 데이터 흐름
- 예외 처리
- 클래스 / 객체 기본 구조
- 동기 / 비동기
- Coroutine / async / await

Python 개념 자체를 다시 처음부터 학습하지 않고, AI 개념을 이해하는 데 필요한 순간에 짧게 재호출합니다.

---

## 14. 2026-08-23 학습 주제 전환

### 변경 사항 1 — 큰 학습 주제 변경

```text
이전
Python 기초

현재
AI Literacy
```

Python은 1차 학습 단계를 마쳤으며, 이번 주부터 AI Literacy를 중심으로 학습합니다.

### 변경 사항 2 — 선예습 도입

Python 과정에서는 강의를 처음 듣는 시점에 새로운 개념을 처음 접하는 경우가 많았습니다.

그 결과:

- 강의 중 용어를 따라가는 데 에너지가 많이 소모됨
- 개별 설명은 이해해도 전체 구조를 동시에 잡기 어려움
- 수업 시간에 질문할 핵심 포인트를 미리 선별하기 어려움
- 복습 시 처음부터 다시 구조를 잡아야 하는 부담이 있었음

따라서 AI Literacy부터는 **전체 교안을 강의 전에 미리 학습 코치에게 제공하고 선예습 자료를 먼저 생성**합니다.

---

## 15. 2026-08-23부터 적용하는 학습 루틴

기본 Workflow:

```text
전체 교안 사전 입력
↓
01_AI_학습_코치가 일자 / 강의별 구조화
↓
선예습
- 전체 구조 확인
- 핵심 용어 확인
- 개념 간 관계 확인
- 예습 질문 생성
↓
실제 강의 수강
↓
강의 시간에 01_AI_학습_코치와 피드백
- 어려운 부분
- 핵심 개념
- 전체 구조
- 강사의 설명과 예습 내용 연결
↓
복습
- 내 말로 설명
- 틀린 이해 수정
- 개념 연결
↓
응용
- 실제 서비스 사례
- 비즈니스 문제
- Python / 향후 LLM·RAG·Agent와 연결
↓
학습 상태 후보 생성
↓
02_학습_아카이브_매니저가 LEARNER_STATE / TIL / WIL / 저장소 구조에 반영
```

핵심 원칙:

> **선예습 → 강의 → 수업 중 구조화·피드백 → 복습 → 응용**

이번 루틴의 목적은 강의 시간 자체를 “처음 개념을 만나는 시간”이 아니라, **이미 본 개념을 연결하고 질문하고 수정하는 시간**으로 바꾸는 것입니다.

---

## 16. 01_AI_학습_코치 역할

01_AI_학습_코치는 **학습 과정 자체**를 담당합니다.

주요 역할:

- 전체 교안 선분석
- 일자별 / 강의별 예습 자료 생성
- 초보자 눈높이 개념 설명
- 개념 간 관계와 전체 구조 설명
- 강의 중 질문 대응
- 이해 오류 수정
- 어려웠던 단계 추적
- 복습 문제와 응용 문제 설계
- Python에서 배운 개념과 AI Literacy 연결
- 반복적으로 나타나는 취약점 후보 생성

01의 핵심 산출물은 단순 요약 문서가 아니라 **현재 학습자가 무엇을 이해했고 무엇을 아직 연결하지 못했는지에 대한 학습 상태 정보**입니다.

---

## 17. 02_학습_아카이브_매니저 역할과 LEARNER_STATE의 목적

`LEARNER_STATE.md`는 단순한 진도표가 아닙니다.

이 파일을 업데이트하는 핵심 목적은 **01_AI_학습_코치에서 발생한 학습 상태를 02_학습_아카이브_매니저가 이어받아 활용할 수 있게 하는 것**입니다.

02_학습_아카이브_매니저는 이 파일을 읽고:

- 일자별 학습 내용과 TIL/WIL을 관리
- 저장소 구조와 학습 기록을 정리
- 현재까지 배운 내용을 장기 관점에서 연결
- 배운 내용을 사용자가 어떻게 실제로 응용할지 함께 논의
- Python → AI Literacy → LLM → RAG → AI Agent로 이어지는 성장 경로를 관리
- 단순 학습 기록을 포트폴리오·프로젝트·실제 문제 해결로 발전시키는 방향을 제안

합니다.

역할 관계:

```text
01_AI_학습_코치
→ 지금 배우는 내용을 이해하도록 돕는다
→ 학습 과정과 이해 상태를 관찰한다

LEARNER_STATE.md
→ 두 역할 사이의 공용 학습 상태 인터페이스

02_학습_아카이브_매니저
→ 누적 학습 기록을 관리한다
→ 배운 내용을 어떻게 응용하고 발전시킬지 사용자와 논의한다
```

따라서 LEARNER_STATE에는 단순히 “무엇을 배웠다”뿐 아니라 다음 정보가 유지되어야 합니다.

- 현재 학습 단계
- 이해 수준
- 반복적으로 막히는 지점
- 효과적이었던 학습 방식
- 최근 학습 전략 변경
- 이전 지식과 새로운 지식의 연결점
- 다음 응용 방향

---

## 18. 현재 복습 및 응용 우선순위

### 우선순위 1 — AI Literacy 전체 지도 만들기

개별 용어를 암기하기 전에:

```text
이 개념은 무엇인가
↓
왜 필요한가
↓
전체 AI 시스템에서 어디에 위치하는가
↓
다른 개념과 어떤 관계인가
↓
실제로 어디에 쓰이는가
```

를 먼저 파악합니다.

### 우선순위 2 — 강의 전 선예습 효과 검증

확인할 것:

- 강의 이해 속도가 실제로 빨라지는가
- 질문의 질이 좋아지는가
- 전체 구조를 놓치는 일이 줄어드는가
- 복습 시간이 줄어드는가

### 우선순위 3 — Python과 AI Literacy 연결

AI 개념을 배울 때 Python에서 이미 본 기술적 요소가 등장하면 연결합니다.

예:

```text
JSON / API / 데이터 구조
비동기 I/O
함수와 입력·출력
객체와 서비스 구조
예외 처리
```

---

## 19. 상태 관리 원칙

이 문서는 현재 상태 요약입니다.

상세 수업 내용 전체를 복사하지 않습니다.

변경 시:

- 기존 학습 이력을 필요한 범위에서 보존
- 현재 학습 단계는 최신 상태로 명확히 갱신
- 이해도가 실제로 변했을 때 상태 변경
- 일회성 질문을 영구 약점으로 기록하지 않음
- 여러 세션에서 반복되는 어려움만 취약점 후보로 승격
- 새로운 학습 전략은 효과를 일정 기간 추적
- TIL과 WIL을 근거 기록으로 활용
- 학습 전략 변경 이유까지 남겨 다음 에이전트가 맥락을 이해할 수 있도록 함

---

## 20. 다음 업데이트 시 확인할 것

- 선예습 → 강의 → 복습 → 응용 루틴이 실제로 효과적인가
- AI Literacy의 개념 지도를 스스로 설명할 수 있는가
- 개별 AI 용어가 아니라 개념 간 관계를 설명할 수 있는가
- Python 과정에서 관찰된 '설명은 이해하지만 처음부터 구조화하기 어려움' 패턴이 AI Literacy에서도 반복되는가
- 강의 중 어려운 부분을 예습 덕분에 더 빨리 식별할 수 있는가
- 배운 AI 개념을 실제 서비스나 비즈니스 문제에 연결할 수 있는가
- 02_학습_아카이브_매니저가 LEARNER_STATE를 바탕으로 응용·프로젝트 방향을 자연스럽게 이어갈 수 있는가
