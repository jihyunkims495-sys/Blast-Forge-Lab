# LEARNER_STATE

Version: 1.0
Last Updated: 2026-08-19
State Owner: 02_학습_아카이브_매니저
Learning Source: 01_AI_학습_코치
Canonical Storage: GitHub / Blast-Forge-Lab

---

## 1. 현재 학습 트랙

```text
Python
↓
SQL
↓
FastAPI
↓
AI Literacy
↓
LLM
↓
RAG
↓
AI Agent Development
```

현재 단계: **Python 기초**

---

## 2. 현재 학습 목표

단순히 Python 문법을 암기하는 것이 아니라 다음 능력을 만드는 것이 목표입니다.

1. 코드를 위에서 아래로 읽을 수 있다.
2. 값이 어느 변수에 저장되는지 추적할 수 있다.
3. 함수 호출과 반환 흐름을 설명할 수 있다.
4. 문제에서 필요한 입력값과 출력값을 찾을 수 있다.
5. 필요한 변수와 연산을 스스로 설계할 수 있다.
6. 조건문·반복문·함수·예외처리 중 필요한 구조를 선택할 수 있다.
7. 오류가 발생한 위치와 원인을 추적할 수 있다.
8. 이전에 학습한 개념을 새로운 문제에 연결할 수 있다.
9. 장기적으로 AI가 작성한 코드를 읽고 평가할 수 있다.

---

## 3. 학습 완료 또는 비교적 안정적인 영역

### Python 기본

상태: `안정 ~ 부분 이해`

- 변수 기본 개념
- 기본 자료형
- 기본 연산자
- 문자열
- 리스트
- 튜플
- 딕셔너리
- 집합
- 기본 인덱싱과 슬라이싱

### 조건문

상태: `부분 이해`

- `if`
- 기본 조건식
- 비교 연산
- 단순한 조건문 읽기

현재 상태:

단순한 `if` 문은 이전보다 읽는 것이 익숙해졌습니다.
다만 반복문, 함수 등 다른 구조와 함께 사용되는 조건문은 추가 연습이 필요합니다.

### 반복문

상태: `부분 이해`

- `for`
- `while`
- `range`
- `break`
- `continue`
- 반복 변수
- 누적 변수의 기본 개념

추가 복습 필요:

- 문제를 보고 반복이 필요한지 판단하기
- 중첩 반복
- `continue`의 실행 흐름
- 필요한 변수 개수 설계

---

## 4. 함수

상태: `부분 이해`

현재 이해한 내용:

- 함수는 반복해서 사용할 기능을 묶은 구조
- 매개변수
- 인수
- 지역변수
- `return`
- 함수 호출 후 반환값이 다른 변수에 저장될 수 있음

추가 복습 필요:

- 문제에서 함수가 필요한지 스스로 판단
- 매개변수와 실제 값의 흐름
- 여러 함수가 연결될 때 실행 순서
- 함수와 메서드의 관계

---

## 5. 클래스 / 객체지향

상태: `학습 중`

### 현재 이해

- `class`는 객체를 만들기 위한 설계도라는 개념
- 클래스 안에는 데이터와 기능을 정의할 수 있음
- 클래스 안에 정의된 함수는 메서드가 될 수 있음
- `.`은 무조건 메서드를 의미하는 것이 아니라 왼쪽 대상이 가진 속성이나 기능에 접근하는 문법
- 클래스 변수는 클래스가 가지는 공용 데이터
- 인스턴스 변수는 각각의 인스턴스가 따로 가지는 데이터

### 아직 부분 이해

- 객체
- 인스턴스
- 변수와 객체의 관계
- `self`
- 인스턴스 메서드
- 클래스 메서드
- `cls`
- `@classmethod`
- `@staticmethod`
- 데코레이터
- 클래스 변수와 인스턴스 변수가 같은 이름을 가질 때의 탐색 방식

### 오늘 확인한 개념

```text
User.count
→ 클래스가 가진 count에 접근

user.count
→ 먼저 해당 인스턴스에서 count를 찾음
→ 없으면 클래스에서 찾을 수 있음

user.count = 10
→ 해당 인스턴스에 별도의 count 속성이 생길 수 있음
```

### 캡슐화 관련

상태: `재확인 필요`

- `__변수명`
- Name Mangling
- 캡슐화와 Name Mangling의 관계

현재 핵심 구분:

```text
캡슐화
→ 객체 내부 데이터 접근을 관리하는 객체지향 개념

Name Mangling
→ __변수명에 대해 Python이 내부 이름을 변경하는 규칙
```

---

## 6. 예외 처리

상태: `학습 중 → 이해 형성 중`

### 현재 이해

- `try`와 `except`는 함수가 아니라 Python 키워드
- `try`에서 예외가 발생할 수 있는 코드를 실행
- 예외가 없으면 `except`는 건너뜀
- 예외가 발생하면 `try`의 남은 코드 실행을 중단하고 맞는 `except`로 이동
- `ValueError`는 값이 해당 연산에 적절하지 않을 때 발생할 수 있음
- `raise`는 개발자가 직접 예외를 발생시키는 키워드

### 현재 이해한 관계

```text
raise
→ 예외를 발생시킴

try
→ 예외가 발생할 수 있는 코드를 실행

except
→ 발생한 예외를 처리
```

### 예외 처리와 디버깅 구분

```text
예외 처리
→ 문제가 생겼을 때 프로그램의 대응 방법을 작성

디버깅
→ 문제가 생긴 이유를 찾아 수정
```

---

## 7. 디버깅

상태: `부분 이해`

현재 이해한 내용:

- 브레이크포인트
- Variables 패널
- Step Into
- Step Out
- 코드 실행 흐름을 단계적으로 확인하는 개념

현재 연결:

Python Tutor처럼 실행 흐름을 시각적으로 추적하는 방식과 IDE/Jupyter Debugger가 비슷한 학습 효과를 줄 수 있다는 점을 이해하고 있습니다.

---

## 8. 내장 함수

상태: `학습 중`

최근 학습:

- `all()`
- `any()`
- `len()`
- `sum()`
- `round()`
- `ceil()`
- `floor()`
- `mean()`
- `median()`
- iterable 개념 복습

추가 확인 필요:

- `all()` / `any()`와 `and` / `or`의 관계
- 빈 iterable에서의 동작
- iterable을 여러 함수와 연결해서 사용하는 방식

---

## 9. 현재 문제 풀이 능력 관찰

### 확인된 강점

- 완성된 간단한 코드를 보면 실행 흐름을 읽을 수 있는 경우가 증가하고 있음
- 오류 메시지의 위치를 함께 보면 원인을 이해하는 능력이 향상되고 있음
- 자신의 말로 개념을 설명하려고 시도함
- 모르는 부분을 넘어가지 않고 질문함

### 현재 집중해서 훈련할 부분

문제를 보고 코드를 작성하기 전에 필요한 연산을 설계하는 과정.

특히:

```text
문제 문장
↓
입력값
↓
출력값
↓
필요한 변수
↓
연산식
↓
필요한 제어구조
↓
코드
```

로 변환하는 훈련이 필요합니다.

---

## 10. 문제 설계 취약 단계

현재 관찰 단계:

### 우선 추적

`연산식 만들기`

사례:

태어난 연도를 입력받아 나이를 계산하는 문제에서
완성된 코드를 보면 이해할 수 있었지만,

```text
현재 연도 - 태어난 연도 = 나이
```

라는 계산 관계를 문제에서 먼저 뽑아내는 과정에서 어려움을 느낌.

### 아직 취약점으로 확정하지 않음

현재는 오늘 확인된 중요한 관찰이며,
향후 여러 문제에서 반복되는지 추가 추적합니다.

---

## 11. 2026-08-19 학습 로직 변경

### 기존 방식

```text
문제 확인
↓
완성 코드 또는 예제 확인
↓
코드 읽기
↓
이해
```

문제점:

코드를 보면 이해하지만,
완성 코드가 없는 새로운 문제에서 변수와 연산을 직접 설계하는 과정이 충분히 훈련되지 않을 수 있음.

### 변경된 방식

```text
문제 확인
↓
입력값 찾기
↓
출력값 찾기
↓
필요한 변수 찾기
↓
연산 관계 만들기
↓
조건 / 반복 / 함수 / 예외 필요 여부 판단
↓
실행 순서 설계
↓
직접 코드 작성
↓
코드 검토
```

### 변경 이유

정답 코드를 읽는 능력과 문제를 코드로 구조화하는 능력은 서로 다른 능력이기 때문입니다.

### 기대 효과

- 문제 해석 능력 향상
- 변수 설계 능력 향상
- 연산 설계 능력 향상
- 제어문 선택 능력 향상
- AI가 작성한 코드를 평가하는 능력 향상
- 새로운 문제에 대한 독립적인 접근 능력 향상

### 추적할 항목

향후 실습 문제마다 다음 중 어디에서 막히는지 확인합니다.

1. 입력값
2. 출력값
3. 변수
4. 연산식
5. 조건
6. 반복
7. 함수
8. 예외
9. 코드 변환
10. 오류 수정

---

## 12. 누적 예제 확장 전략

새로운 개념을 학습할 때 이전에 안정적으로 배운 개념을 1~2개 연결합니다.

예:

```text
변수
↓
조건문
↓
반복문
↓
함수
↓
예외처리
↓
클래스
```

모든 개념을 한꺼번에 넣지 않습니다.

현재 부분 이해 상태인 클래스 개념은 다른 새 개념의 복잡한 예제에 무리하게 섞지 않습니다.

---

## 13. 다음 학습에서 우선 연결할 개념

다음 Python 학습 예제를 만들 때 우선 연결 가능:

- 변수
- 기본 자료형
- `input()`
- `int()`
- 기본 조건문
- 간단한 반복문
- 기본 함수
- `return`

주의해서 사용할 개념:

- 객체
- 인스턴스
- 클래스 메서드
- 정적 메서드
- 데코레이터

위 개념은 아직 학습 중이므로 새로운 복잡한 예제의 전제조건으로 사용하지 않습니다.

---

## 14. 다음 복습 포인트

우선순위 1:

```text
문제 → 입력 → 출력 → 변수 → 연산
```

설계 연습

우선순위 2:

클래스 관계 정리

```text
class
object
instance
variable
attribute
method
self
cls
```

우선순위 3:

예외 처리 실행 흐름

```text
try
→ 정상 / 예외 분기
→ except
→ raise
```

---

## 15. 현재 학습 운영 구조

2026-08-20부터 적용할 기본 Workflow:

```text
01_AI_학습_코치
↓
교안 입력
↓
수업 학습
↓
질문
↓
복습
↓
실습 문제 설계
↓
코드 작성
↓
오류 분석
↓
"오늘의 학습내용 정리해줘"
↓
LEARNER_STATE 업데이트 후보 생성
↓
사용자가 02 프로젝트로 전달
↓
02가 기존 canonical LEARNER_STATE와 비교
↓
TIL / WIL / 상태 변화 검토
↓
최종 LEARNER_STATE 수정안
↓
사용자 승인
↓
GitHub 반영
```

---

## 16. 상태 관리 원칙

이 문서는 현재 상태 요약입니다.

상세 수업 내용 전체를 복사하지 않습니다.

변경 시:

- 기존 상태를 삭제하지 않고 필요한 항목을 유지
- 이해도가 실제로 변했을 때 상태 변경
- 일회성 질문을 영구 약점으로 기록하지 않음
- 새로운 학습 전략은 효과를 일정 기간 추적
- TIL과 WIL에서 근거를 확인

---

## 17. 다음 업데이트 시 확인할 것

- 설계 우선 문제 풀이 방식이 실제로 도움이 되는가
- 연산식 설계에서 반복적으로 막히는가
- 클래스와 객체 관계 이해가 개선되었는가
- 예외처리 문제를 스스로 설계할 수 있는가
- 이전 개념을 활용한 확장 예제가 과도하게 어렵지 않은가
